<h2><%= link_to 'Tracks', :tracks %> > Create Track</h2>

<%= form_for(@track) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
